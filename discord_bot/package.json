{
  "name": "bonfire-discord-bot",
  "version": "2.0.0",
  "description": "Discord bot for controlling game servers on AWS EC2",
  "main": "src/index.js",
  "scripts": {
    "test": "echo \"No tests configured\"",
    "build": "rm -f bonfire_discord_bot.zip && npm ci --omit=dev && zip -r bonfire_discord_bot.zip node_modules src/index.js",
    "register-commands": "npm install && node register-commands.js",
    "register-commands:global": "npm install && node register-commands.js --global"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1354.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "discord.js": "^14.18.0",
    "dotenv": "^16.4.5"
  }
}
